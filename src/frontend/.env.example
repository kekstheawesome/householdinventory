# Override the backend API base URL (defaults to the Vite dev-server proxy path '/api').
# Set this when running the frontend against a remote or differently-hosted backend.
# VITE_API_BASE_URL=http://localhost:5000/api
